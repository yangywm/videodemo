import { Filter } from "./filter";
/** 智能文件夹 */
export declare namespace SmartFolder {
    namespace Ext {
        interface ObjValue extends Filter.Ext.ObjValue {
            [Filter.Ext.ObjValueOfKey.properties]?: {
                id: string;
                title: string;
            };
            /** 关系 0=并且 1=或者 */
            logic?: FilterFieldEnum;
        }
        type ItemObject = {
            [k in Filter.Ext.ObjKey]?: ObjValue[];
        };
        interface Create {
            /** 扩展信息 */
            properties: Properties;
            /** 智能文件名称 */
            title: string;
        }
        enum FilterFieldEnum {
            /** 并且 */
            filter = 0,
            /** 或者 */
            should = 1
        }
        interface Properties {
            /** 关系 0=并且 1=或者 */
            logic: FilterFieldEnum;
            /** 过滤标签 */
            filter: ItemObject[];
            /** 描述 */
            description?: string;
        }
    }
    interface Item extends Ext.Create {
        created_at: string;
        id: string;
        org_id: string;
    }
    interface Response {
        data: Item[] | null;
        total: number;
    }
}
