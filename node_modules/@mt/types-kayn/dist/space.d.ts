import { OrgMember } from "./orgMember";
export declare namespace Space {
    interface BaseItem {
        id: string;
        root_asset_id: string;
        org_id: string;
    }
    interface Item extends BaseItem {
        /** ~~空间的重命名（可能会用到）~~ */
        rname: string;
        /** 空间描述 */
        description: string;
        /** 空间类型
         * - 0:个人空间
         * - 1:团队公共空间
         * - 2:部门空间
         * - 3:自定义空间
         */
        space_type: string;
        created_at: string;
    }
    interface LibraryItem extends Omit<BaseItem, "org_id"> {
        title: string;
        space_id: string;
        total: number;
        member: {
            id: string;
            role: OrgMember.Ext.Role;
        };
    }
}
