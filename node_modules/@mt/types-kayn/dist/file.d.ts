import { Base } from "./base";
import { FileInfo } from "./fileInfo";
export declare namespace File {
    namespace Ext {
        /** 转码 */
        interface Transcode {
            /** 转码流，需要签名 */
            file: string;
            /**
             * 源流
             * - Video: 'LD', 'SD', '540P', 'HD', 'FHD', '2K', '4K', 'ORIGNAL' 老数据, 'ORIGINAL','ORIGINAL_RES'
             * - Audio: '320', '192', '160', '128', '64'
             */
            res: string;
            /** success | fail */
            state: string;
            /** 是否有权益观看 */
            has_rights: boolean;
        }
        /** 音频波形图 */
        interface Wave {
            /** 封面地址 */
            cover: string;
            wave: string;
        }
        /** 代理视频文件 */
        interface VideoProxies {
            res: string;
            uri: string;
        }
    }
    interface Item extends Base.Item {
        /** 封面地址 */
        cover: string;
        /** 是否为私有 */
        decoded: boolean;
        /** 音视频时长，单位秒(s) */
        duration: number;
        /** 文件的MD5信息摘要 */
        hash: string;
        /** mediaInfo */
        info: FileInfo.MediaInfo | FileInfo.ImageInfo | null;
        /** 矫正后的媒体类型 */
        mime: string;
        /** 文件大小（单位：B） */
        size: string;
        /** 视频快照地址 */
        snapshot: string;
        /** 云存储通地址，需要token和header */
        src: string;
        /** 视频转码结果 */
        transcodes: Ext.Transcode[];
        /** 音频波形图结果 */
        waves: Ext.Wave[];
        object: string;
        /**
         * {@link VideoProxies}
         */
        video_proxies: Ext.VideoProxies[];
        /** 冻结 */
        freeze: boolean;
    }
    interface ItemLite extends Base.Item {
        /** 矫正后的媒体类型 */
        mime: string;
        /** 冻结 */
        freeze: boolean;
        /** 音视频时长，单位秒(s) */
        duration: number;
        /** 视频快照地址 */
        snapshot: string;
        /** 封面地址 */
        cover: string;
        /** mediaInfo */
        info: FileInfo.MediaInfo | FileInfo.ImageInfo | null;
    }
}
